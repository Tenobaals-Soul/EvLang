public struct my_struct {
    int my_int;
    char my_char = 5;
    String my_string;
}

i++;

private namespace not_yours {
    struct my_struct {
        int my_int;
        char my_char = 5;
        String my_string;
    }
    for int i = 0; i < 5, true; i++ {
        my_struct var = {i, 'a' + i, "hello world"};
        print("var:", var);
    }
}

int i = 0;